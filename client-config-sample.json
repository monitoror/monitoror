{
  "version": 1,
  "columns": 4,
  "tiles": [
    { "type": "EMPTY" },
    { "type": "PING", "params": { "hostname": "localhost" } },
    { "type": "PORT", "params": { "hostname": "localhost", "port": 8080 } },
    { "type": "GROUP", "label": "...", "tiles": [
      { "type": "PING", "params": { "hostname": "localhost" } },
      { "type": "PORT", "params": { "hostname": "localhost", "port": 8080 } }
    ]}
  ]
}
