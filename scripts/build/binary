#!/usr/bin/env bash
#
# Build a static binary for Linux
#

set -eu -o pipefail

source ./scripts/.variables

export CGO_ENABLED=0
export GOOS=linux
export GOARCH=amd64
export GOGC=off

TARGET="build/monitoror-$GOOS-$GOARCH"

echo "Building statically linked $TARGET"
go build -o "${TARGET}" --ldflags "${LDFLAGS}" "${SOURCE}"
