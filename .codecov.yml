ignore:
  - cmd/

fixes:
  - "github.com/monitoror/::"

coverage:
  status:
    project:
      default: off
      front:
        flags: front
      back:
        target: 70%
        flags: back

flags:
  backe:
    paths:
      - front/
  front:
    paths:
      - !front/
